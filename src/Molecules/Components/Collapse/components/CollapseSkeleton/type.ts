import {CollapseSkeletonSizes} from "../../type";

export const iconSizes: Record<CollapseSkeletonSizes, number> = {
    [CollapseSkeletonSizes.EXTRA_SMALL]: 24,
    [CollapseSkeletonSizes.SMALL]: 25,
    [CollapseSkeletonSizes.DEFAULT]: 25,
};
